# Go through ArcGIS JS API and others

## Building and running on localhost

First install dependencies:

```sh
npm install
```

To create a production build:

```sh
npm run build
```

Develop:

```sh
npm run dev
```
